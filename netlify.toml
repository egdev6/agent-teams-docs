[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "20"

# Redirect bare 404s to Docusaurus 404 page
[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404
